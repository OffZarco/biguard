{% extends 'base.html.twig' %}

{% block title %}Questions{% endblock %}

{% block body %}
    <h1>Questions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ question.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ question.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_questions_index') }}">back to list</a>

    <a href="{{ path('app_admin_questions_edit', {'id': question.id}) }}">edit</a>

    {{ include('admin/questions/_delete_form.html.twig') }}
{% endblock %}
